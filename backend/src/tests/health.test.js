import { test } from "node:test";
import assert from "node:assert";

// Placeholder smoke test — expand with supertest once you want real API tests.
test("sanity check", () => {
  assert.strictEqual(1 + 1, 2);
});
